# 令牌刷新页面优化

## 概述

优化了客户端的令牌刷新体验，当访问令牌过期需要刷新时，不再直接跳转到登录页面，而是：

1. 跳转到专门的令牌刷新页面
2. 在该页面显示刷新进度
3. 刷新成功后自动返回之前的页面
4. 刷新失败时让用户选择重试或重新登录

## 实现的功能

### 1. 令牌刷新页面 (`AuthTokenRefreshPage`)

**文件位置**: `/lib/page/auth/token_refresh/auth_token_refresh_page.dart`

**功能特性**:

- ✅ 美观的 UI 设计，带有渐变背景和卡片布局
- ✅ 显示刷新进度（加载动画）
- ✅ 刷新成功：显示成功图标，自动返回之前的页面
- ✅ 刷新失败：显示错误图标和重试次数
- ✅ 提供两个操作按钮：
    - **重试按钮**: 重新尝试刷新令牌
    - **重新登录按钮**: 清除令牌并跳转到登录页

**参数**:

- `returnPath` (可选): 刷新成功后返回的页面路径，通过 QueryParam 传递

**UI 状态**:

1. **正在刷新**: 显示加载动画和"正在刷新登录令牌..."
2. **刷新成功**: 显示绿色勾选图标和"令牌刷新成功！正在返回..."
3. **刷新失败**: 显示红色错误图标、失败原因和操作按钮

### 2. AuthStore 增强

**文件位置**: `/lib/store/service/auth/auth_store.dart`

**新增功能**:

#### 2.1 新增回调函数

```dart
// 跳转到令牌刷新页回调
void Function(String? returnPath)? onNavigateToTokenRefresh;

// 保存的返回路径
String? _savedReturnPath;
```

#### 2.2 新增方法

```dart
// 保存当前路径，用于刷新成功后返回
void saveCurrentPath(String path) {
  _savedReturnPath = path;
  debugPrint('💾 保存返回路径: $path');
}

// 跳转到令牌刷新页面
void _navigateToTokenRefresh() {
  if (onNavigateToTokenRefresh != null) {
    debugPrint('🔄 触发跳转到令牌刷新页');
    onNavigateToTokenRefresh!(_savedReturnPath);
  } else {
    debugPrint('⚠️ 无法跳转到令牌刷新页（回调未设置）');
    _navigateToLogin();
  }
}
```

#### 2.3 修改 `apiRefreshToken` 方法

- 新增 `skipNavigation` 参数，允许在后台静默刷新
- 网络错误时不再自动清除令牌，而是让用户选择重试
- 只有在认证错误（401/403）时才清除令牌

```dart
Future<bool> apiRefreshToken({bool skipNavigation = false}) async {
  // ...existing code...

  // 401/403 错误 - 刷新令牌无效
  if (e.response?.statusCode == 401 || e.response?.statusCode == 403) {
    if (!skipNavigation) {
      await logout();
      _navigateToLogin();
    }
    return false;
  }

  // 其他网络错误 - 不自动登出，让用户选择
  if (!skipNavigation) {
    _showErrorSnackBar('网络错误，请重试');
  }
  return false;
}
```

#### 2.4 修改定时器刷新逻辑

- 定时器触发的令牌刷新失败时，跳转到令牌刷新页面
- 使用 `skipNavigation: true` 避免在后台自动跳转到登录页

```dart
_refreshTokenTimeout = Timer
(
timeout, () async {
final success = await apiRefreshToken(skipNavigation: true);
if (success) {
_startRefreshTokenTimer();
} else {
_navigateToTokenRefresh();
}
});
```

### 3. 路由配置

**文件位置**: `/lib/router/app_router.dart`

**新增路由**:

```dart
AutoRoute
(
page: AuthTokenRefreshRoute.page,
path: 'auth/token_refresh',
)
,
```

### 4. 主应用配置

**文件位置**: `/lib/main.dart`

**新增回调设置**:

```dart
// 设置跳转到令牌刷新页回调
AuthStore
().onNavigateToTokenRefresh = (
String? returnPath) {
final path = returnPath ?? '/';
appRouter.pushPath('/auth/token_refresh?returnPath=${Uri.encodeComponent(path)}');
};
```

**MyApp 改为 StatefulWidget**:

```dart
class MyApp extends StatefulWidget {
  const MyApp({super.key});

  @override
  State<MyApp> createState() => _MyAppState();
}

class _MyAppState extends State<MyApp> {
  @override
  void initState() {
    super.initState();

    // 在首帧渲染后检查是否需要跳转到令牌刷新页面
    WidgetsBinding.instance.addPostFrameCallback((_) {
      AuthStore().checkAndHandleStartupRefresh();
    });
  }

// ...build method...
}
```

## 用户体验流程

### 场景 1: 应用启动时令牌过期（新增）

1. 用户打开应用
2. `AuthStore.init()` 检测到有刷新令牌但没有访问令牌
3. 自动尝试刷新访问令牌
4. 如果刷新失败（网络问题或令牌过期）：
    - 标记 `_needsTokenRefreshOnStart = true`
    - 等待 UI 初始化完成
5. 首帧渲染后，自动跳转到令牌刷新页面
6. 用户可以选择：
    - **重试**: 再次尝试刷新令牌
    - **重新登录**: 清除所有数据并跳转到登录页

### 场景 2: 定时器触发的自动刷新失败

1. 用户正在浏览某个页面（例如：`/user/dashboard`）
2. 访问令牌即将过期，定时器触发刷新
3. 刷新失败（网络问题或令牌无效）
4. **自动跳转**到令牌刷新页面
5. 显示刷新进度和结果
6. 用户可以选择：
    - **重试**: 再次尝试刷新令牌
    - **重新登录**: 清除所有数据并跳转到登录页

### 场景 3: API 调用时发现令牌过期

1. 用户在某个页面执行操作，触发 API 调用
2. API 返回 401 错误（令牌过期）
3. Dio 拦截器自动尝试刷新令牌
4. 如果刷新失败，跳转到令牌刷新页面
5. 后续流程同场景 2

### 场景 4: 刷新成功

1. 用户在令牌刷新页面
2. 刷新成功后显示"令牌刷新成功！正在返回..."
3. 500ms 后自动跳转回之前的页面（如果有保存返回路径）
4. 或跳转到首页（如果没有返回路径）

## 技术细节

### 1. 路径参数传递

使用 `@QueryParam` 而不是 `@PathParam`，因为返回路径可能包含特殊字符：

```dart
@RoutePage()
class AuthTokenRefreshPage extends StatefulWidget {
  const AuthTokenRefreshPage({
    super.key,
    @QueryParam('returnPath') this.returnPath,
  });

  final String? returnPath;
}
```

跳转时使用 URL 编码：

```dart
appRouter.pushPath
('/auth/token_refresh?returnPath=
${Uri.
encodeComponent
(
path
)
}
'
);
```

### 2. 应用启动时的令牌刷新处理

**问题**：应用启动时如果令牌需要刷新，此时 UI 还未完全初始化，不能立即跳转到刷新页面。

**解决方案**：使用标志位 + PostFrameCallback

```dart
// 在 AuthStore 中
bool _needsTokenRefreshOnStart = false;

Future<void> init() async {
  _prefs = await SharedPreferences.getInstance();
  await _refreshThisToken();

  if (_accessJWTToken == null && _refreshJWTToken != null) {
    debugPrint('🔄 应用启动时检测到需要刷新访问令牌');
    final success = await apiRefreshToken(skipNavigation: true);

    if (!success) {
      debugPrint('⚠️ 应用启动时令牌刷新失败，标记需要跳转到刷新页面');
      _needsTokenRefreshOnStart = true; // 标记需要跳转
    }
  }

  // ...
}

void checkAndHandleStartupRefresh() {
  if (_needsTokenRefreshOnStart) {
    debugPrint('🔄 处理应用启动时的令牌刷新失败，跳转到刷新页面');
    _needsTokenRefreshOnStart = false;
    _navigateToTokenRefresh();
  }
}
```

在 MyApp 中的首帧后调用：

```dart
@override
void initState() {
  super.initState();

  WidgetsBinding.instance.addPostFrameCallback((_) {
    AuthStore().checkAndHandleStartupRefresh();
  });
}
```

**流程**：

1. `main()` → `AuthStore().init()` → 检测到需要刷新 → 后台刷新（skipNavigation: true）
2. 刷新失败 → 设置标志位 `_needsTokenRefreshOnStart = true`
3. `runApp(MyApp())` → UI 开始构建
4. `MyApp.initState()` → 注册 PostFrameCallback
5. 首帧渲染完成 → 触发回调 → `checkAndHandleStartupRefresh()`
6. 检查标志位 → 跳转到令牌刷新页面

这样确保跳转发生在 UI 完全初始化之后，避免路由错误。

### 3. 刷新重试机制

- 记录重试次数，显示给用户
- 每次重试都是完整的刷新流程
- 不限制重试次数，由用户决定何时放弃

### 3. 避免循环跳转

- 使用 `skipNavigation` 参数避免在后台刷新时自动跳转
- 只在定时器触发的刷新失败时才跳转到刷新页面
- 手动调用刷新失败时（在刷新页面内），不会再次跳转

### 4. 网络错误处理优化

**之前的逻辑**:

- 任何刷新失败都清除令牌并跳转到登录页

**现在的逻辑**:

- **401/403 错误**: 刷新令牌确实无效，清除令牌并跳转到登录页
- **网络错误**: 可能是临时问题，不清除令牌，让用户选择重试
- **其他错误**: 安全起见，清除令牌并跳转到登录页

## UI 设计

### 颜色方案

- **主题色**: 深紫色 (`Colors.deepPurple`)
- **成功色**: 绿色 (`Colors.green.shade400`)
- **错误色**: 红色 (`Colors.red.shade400`)
- **背景**: 深紫色渐变

### 布局

- 全屏渐变背景
- 居中显示白色卡片
- 卡片内包含：
    - 状态图标（80x80）
    - 状态文字
    - 额外信息（错误时）
    - 操作按钮（错误时）

### 响应式设计

- 使用 `SingleChildScrollView` 防止小屏幕溢出
- 按钮采用 `Expanded` 平均分配空间
- 使用 `SafeArea` 避免刘海屏问题

## 代码规范遵循

✅ **缩进**: 2 空格  
✅ **命名**: 使用小驼峰命名变量，大驼峰命名类  
✅ **类型声明**: 所有变量都明确类型  
✅ **布尔值**: 使用 `is` 前缀  
✅ **尾逗号**: 在长代码中使用尾逗号  
✅ **const**: 为不可变的 Widget 添加 `const`  
✅ **注释**: 关键逻辑添加中文注释  
✅ **单例模式**: AuthStore 使用单例模式

## 文件清单

### 新增文件

1. `/lib/page/auth/token_refresh/auth_token_refresh_page.dart` - 令牌刷新页面

### 修改文件

1. `/lib/store/service/auth/auth_store.dart` - AuthStore 增强
2. `/lib/router/app_router.dart` - 添加路由配置
3. `/lib/main.dart` - 设置回调函数

### 生成文件

1. `/lib/router/app_router.gr.dart` - 自动生成的路由代码（通过 build_runner）

## 测试建议

### 手动测试场景

1. **正常刷新流程**
    - 登录后等待访问令牌快过期
    - 观察是否自动跳转到刷新页面
    - 确认刷新成功后返回原页面

2. **网络错误重试**
    - 断开网络连接
    - 触发令牌刷新
    - 在刷新页面点击"重试"
    - 恢复网络后再次重试

3. **刷新令牌过期**
    - 手动清除刷新令牌或使用过期的令牌
    - 触发刷新
    - 确认跳转到登录页

4. **返回路径保存**
    - 在不同页面触发令牌刷新
    - 确认刷新成功后返回正确的页面

## 后续优化建议

1. **添加倒计时提示**: 在令牌即将过期前 1 分钟显示提醒
2. **添加手动刷新按钮**: 在用户设置页面添加手动刷新令牌的选项
3. **记录刷新历史**: 在 Debug 页面显示令牌刷新历史和失败原因
4. **添加离线检测**: 检测到离线时不触发刷新，避免无效请求
5. **优化动画**: 添加页面切换动画，提升视觉体验

## 总结

这次优化大幅提升了令牌刷新的用户体验：

- ✅ 用户可以清楚看到刷新状态
- ✅ 网络问题时可以重试，不必重新登录
- ✅ 刷新成功后无缝返回之前的页面
- ✅ 提供了清晰的错误信息和操作选项
- ✅ 避免了因临时网络问题导致的强制登出

这是一个更加人性化和健壮的认证流程。

